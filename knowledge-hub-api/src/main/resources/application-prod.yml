springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

spring:
  docker:
    compose:
      enabled: false
  ai:
    model:
      chat: openai
      embedding: openai

app:
  storage:
    access-key: ${MINIO_ACCESS_KEY:}
    secret-key: ${MINIO_SECRET_KEY:}
